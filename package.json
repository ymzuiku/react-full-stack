{
  "name": "react-full-stack",
  "version": "0.0.1",
  "description": "Isomorphic: Next.js and Koa Server Separate",
  "main": "shipitfile.js",
  "scripts": {
    "install-all": "yarn && cd client && yarn && cd ../server && yarn",
    "deploy": "shipit dev deploy",
    "push": "git add . && git commit -m 'shipit post' && git push && yarn deploy",
    "server": "cd server && yarn server",
    "nodemon": "cd server && nodemon server.js",
    "web": "cd client && yarn web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "shipit-cli": "^4.1.1",
    "shipit-deploy": "^4.1.1"
  }
}
